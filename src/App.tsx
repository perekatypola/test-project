import React from 'react';
import {SafeAreaView} from 'react-native';
import Navigator from './Navigation';

const App: React.FC = () => (
  <SafeAreaView>
    <Navigator />
  </SafeAreaView>
);

export default App;
